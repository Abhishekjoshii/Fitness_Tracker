apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mongodb-pvc
  namespace: fitness

spec:
  storageClassName: gp2
  accessModes:
    - ReadWriteOnce    
  resources:
    requests:
      storage: 5Gi
